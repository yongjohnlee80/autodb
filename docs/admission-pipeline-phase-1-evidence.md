# ADR-0096 phase 1 admission evidence

This is the consolidated Step 9 evidence package for acceptance requirements
A1-A27. Each table row corresponds to one numbered requirement in the parent
task. Compound requirements keep their separately mutated properties in that
numbered row. A review-recorded RED summary is identified as such when the
historical review did not retain the complete test output.

## Evidence sources

- Parent task: `$KB_ROOT/.todo-list/in-progress/2026-09-11-autodb-refactor-to-one-admission-pipeline-legacy-guards-behind-the-stage-interface-adr-0096-phase-1-behaviour-preserving.md`.
- Attached reviews: every document in the parent task's `review:` array.
- Step 8 approved head: `b24dd9ff6fb176930290e4b7050b6ca0de47941d`.
- Historical Step 8 VM43 result, preserved by the parent task and Wanda review: fresh database, clean tree, 1338 passed, 2 expected corpus skips, `HARNESS_EXIT: 0`. The mutable log directory was subsequently reused, so it is not cited as a durable copy of that result.
- Step 9 mutations below were executed against the current worktree, inverse-patched immediately, and followed by their named green witnesses. The final clean exact-head harness remains the release ledger and is intentionally commit-keyed rather than hard-coded here.

## Acceptance evidence

| ID | Claim | Production mutation | Witness | Observed RED message | Restore proof | Restored green | Environment/source |
|---|---|---|---|---|---|---|---|
| `A1` | The existing suite remains unchanged on a fresh review database. | Delete Execute-time `runClassAdmission` from `Engine.WireExecutePortal`. | Existing Parse/Execute authority cells and the full harness census. | `(nil, wanted auth.ErrDenied)`; the pre-existing authority cell detects the missing check without accommodation. | Parent Step 6 records inverse patch and green; final Step 8 review independently accepted the restored tree. | Historical VM43 Step 8 result: 1338 passed, 2 expected corpus skips, `HARNESS_EXIT: 0`; the final Step 9 commit uses a new exact-head ledger. | Parent Steps 6 and 8; Wanda Step 8 review; final commit-keyed harness report. |
| `A2` | Real corpus replay remains byte-identical to the committed manifest. | In `core/exec/profile.go`, admit the v1compat data-modifying-CTE arm. | `TestCorpusReplay` against the real LM deployment corpus and the committed manifest. | `000005_update_backfill_contacts.sql statement 3` changed from `refused:nested-mutation` to `admitted`; admitted count changed 2782 to 2783. | The profile arm was inverse-patched; the manifest was not regenerated. | Replay returned to 2782 admitted, 780 control, 1 nested, and 14 WHERE refusals with the committed manifest SHA-256 below. | Current Step 9 local mutation; real corpus section below. |
| `A3` | Every legacy refusal identity survives its adapter. | Omit size-cap, profile, reader, authorization, WHERE, and session-state stages one at a time from their composed chain. | Adapter identity and stage-omission cells in `admission_stages_test.go`. | Review-recorded RED summary: each omitted adapter admitted the input or stopped producing its legacy `errors.Is` identity. | Each stage was restored before the next omission; Lector independently checked the restored adapter set. | Step 2 targeted suite and later full VM43 suites passed. | Parent Step 2; Lector Step 2 review. |
| `A4` | Stage order is declared and its first refusal is identical across surfaces. | Move `guardWhereStage` before `authorizeUnitStage` in the production session chain; earlier steps also restored reader-before-profile on wire drives. | Declared-order, double-violation, cross-surface, and critical-slice chain cells. | `ordinary session policy stages moved or disappeared: got "profile -> readeranalysis -> guardwhere -> authorizeunit", want "profile -> readeranalysis -> authorizeunit -> guardwhere"`; historical wire reorder exposed the reader identity instead of `ErrStatementUnsupported`. | Current reorder was inverse-patched; earlier drive reorders were restored at their reviewed heads. | Chain rendering, parity, and live collision witnesses pass. | Current Step 9 mutation; parent Steps 3-5; Lector Steps 3-4 and Jarvis Step 5 reviews. |
| `A5` | The same script and connection produce the same denial through front-door and RPC/TUI surfaces. | Bypass `runSessionAdmission` only for the internal-session path. | `TestSessionDrive_CrossSurfaceParityWithPooled` and real wire collision cells. | `cross-surface parity broken: pooled = exec: statement is not supported ... session = exec: statement failed: SQL logic error: near "DELETE": syntax error`. | The internal-session admission call was restored. | Cross-surface parity and focused admission suites pass. | Current Step 9 mutation; `core/exec/admission_stages_test.go`. |
| `A6` | Every executable ingress uses the orchestrator and no legacy guard survives outside an adapter. | Restore a direct raw size check in `Engine.run`, and separately sever canonical reader/authorization adapter delegation. | `TestAdmissionIngressClosure_NoLegacyPolicyGuardCallsOutsideAdapters` and canonical call-count cells. | Review-recorded RED included `0 calls, wanted 1`; the direct drive guard was also rejected by the closure walk. | Mutations were inverse-patched at the Step 6 head. | Closure and adapter tests passed on VM43 at `70cddea` and in later suites. | Parent Step 6; Jarvis Step 6 review. |
| `A7` | Every profile/surface chain is printable and compared with committed production-derived output. | Move `guardWhereStage` before `authorizeUnitStage` in `sessionAdmissionStages`. | `TestAdmissionChainComparison_CurrentBlockMatchesProduction`, `TestAdmissionChainRendering_CriticalStageSlices`, and shared-factory checks. | The current-block byte comparison and critical-slice test both reported the reordered `profile -> readeranalysis -> guardwhere -> authorizeunit` chain. | The factory order was restored; renderer and production still share the same factory. | All chain-render tests pass against `docs/admission-chain-comparison.md`. | Current Step 9 mutation and machine-checked chain section below. |
| `A8` | Authority is recomputed between Parse and Execute and between Executes of one portal. | Delete Execute-time `runClassAdmission` so authority is reused from Parse. | `TestExtPG_GrantRevokedBetweenParseAndExecuteRefuses` and `TestExtPG_GrantDemotedBetweenTwoExecutesOfOnePortalRefuses`. | Both authority boundaries RED in the Step 6 mutation; retained excerpt: `(nil, wanted auth.ErrDenied)` on the second Execute. | The Execute-time class stage was restored before the clean Step 6 ledger. | Both live authority cells passed in the Step 6 and Step 8 VM43 suites. | Parent Step 6; Jarvis Step 6 review. |
| `A9` | Every statement effect follows its durable attempt record. | Move per-statement read-only enforcement before `recordAttemptTagged`. | Attempt ordering and history cells. | `history rows = 0, want exactly one attempted statement`. | Attempt recording was restored ahead of enforcement. | Attempt/outcome ordering and enforcement disposition cells pass. | Current Step 9 mutation; `core/exec/unitpolicy_disposition_test.go`. |
| `A10` | A refused Nth statement leaves no effect from statements 1 through N-1. | Ignore a later `gateWireStatement` refusal and continue the raw plan with the admitted prefix. | `TestWireQueryRaw_OneRefusedStatementRefusesTheWholeBufferBeforeDispatch`. | VM43 reported the buffer accepted, one dispatch of `INSERT INTO raw_gate_2 VALUES (1)`, one emitted message, and `count = [[1]] ... want 0; the admitted prefix took effect before the later refusal`. | The gate loop was restored to return immediately on every refusal. | The strengthened live cell passed on a fresh VM43 review database after restoration. | Current Step 9 VM43 mutation; `core/exec/wire_query_pg_test.go`. |
| `A11` | Every discovered production admission-refusal return routes through the audit sink, and every registered denial code is recordable there. | Return `admitErr` directly from `wireQueryRaw`, bypassing its rejection recorder. | `TestAdmissionRefusalPathsCannotBypassProductionSink`, `TestEveryRegisteredAdmissionDenialCodeIsRecordable`, and production behavioral refusal cells. | `Engine.wireQueryRaw (wire_query.go) returns admission refusal admitErr without a production record sink`. | The `rejectSession` path was restored. | Structural sink routing, registration-derived code recording, and behavioral refusal cells pass. The registration-derived cell validates the real orchestrator and sink with synthetic stage inputs; it does not claim to invoke every production adapter. | Current Step 9 mutation; `core/exec/admission_record_sink_test.go`; gate-matrix production ingress cells. |
| `A12` | Exposure remains fail-closed across default, migration, deny-before-dial, RPC independence, orthogonality, plumbing, and admin authority. | Seven mutations: default migration value `0 -> 1`; backfill predicate `session -> v1compat`; move the exposure check to the reservation boundary; gate internal execution on exposure; restore profile-based reachability; route exposure through profile plumbing; remove admin authorization. | Migration, wire-open ordering, four-quadrant, RPC/TUI, AST boundary, and admin-only cells. | Default mutation: `legacy-closed ... =1, want 0` and `new connection ... =1, want 0`; migration summary: legacy rows reversed; ordering mutation: `exposure refusal reached the reservation boundary 1 time(s)`; review-recorded internal, quadrant, plumbing, and non-admin mutations each RED their named cell. | Current default and ordering changes were inverse-patched; Steps 7a-7c record restoration of the other five mutations. | SQLite migration, wire refusal, four-quadrant, RPC/TUI, plumbing, and admin tests pass; VM43 covers the PostgreSQL migration arm. | Current Step 9 mutations; parent Steps 7a-7c; Jarvis and Wanda exposure reviews. |
| `A13` | Pooled allowlist/mandatory-LOCAL and wire denylist GUC models remain distinct. | Make `sessionStateStage` select one GUC model for both physical contexts. | `TestSessionStateAdapter_TwoGUCModelsDistinct`. | Review-recorded RED summary: `temp_buffers` loses pooled-refused/wire-admitted parity in one direction; `search_path` loses pooled-refused/wire behavior in the other. | Both flattening directions were restored during Step 2. | The bidirectional adapter cell and subsequent VM43 suites pass. | Parent Step 2; Lector Step 2 review. |
| `A14` | Operational stage failure cannot become statement denial. | Convert `Stage.Apply` errors into a deny contribution instead of returning `admission.OperationalError`. | `TestA14_AnErrorIsNeverADenial` and operational-opacity cells. | `the broken stage's error was swallowed - operational failure must reach the caller as itself`. | Error propagation and typed operational wrapping were restored. | Core admission and surface operational-error tests pass. | Current Step 9 mutation; `core/admission/admission_test.go`. |
| `A15` | A second implementation composes and its report is heard without drive edits. | Reject the test's non-legacy `Facts` implementation inside `Orchestrator.Run`. | `TestA15_ASecondImplementationComposes`. | `admission: unsupported facts implementation admission.fakeFacts`. | The concrete-type restriction was removed. | The fake analyzer-stage composition cell passes. | Current Step 9 mutation; `core/admission/admission_test.go`. |
| `A16` | Every discovered executable drive has the sole orchestrator in its production call graph; direct dispatches in classifying/Execute drives follow their local admission calls. | Add exported `Engine.UnsafeAdmissionBypass` that dispatches directly without admission. | `TestAdmissionIngressClosure_AllDiscoveredDrivesReachOrchestrator` plus the four production ingress gate-matrix cells. | `executable drive Engine.UnsafeAdmissionBypass (engine.go) cannot reach the orchestrator`. | The temporary drive was removed. | Static discovery, exemption validation, direct-call ordering, and known-ingress assertions pass. Pooled, RPC-session, and decoded wire-simple production cells pass locally; the PostgreSQL wire-extended cell is conditional on `TEST_PGURL` and remains part of the final VM43 gate. The static graph check is reachability plus direct-site ordering, not whole-program interprocedural branch dominance. | Current Step 9 mutation; `core/exec/admission_ingress_test.go`; `core/exec/gate_matrix_test.go`. |
| `A17` | Dynamic checks share the frame claim; candidate send-capable Parse acquires the segment wrap; Sync releases it. | Release the claim before admission; pass `willSend=false` from Parse; omit segment-wrap release from Sync. | Claim-session, rejected/empty/control Parse, second-frame, and Sync lifetime cells. | Review-recorded RED summary: stale state became observable; candidate Parse failed to acquire the wrap; and the next segment inherited the stranded wrap. | Each ownership/lifetime mutation was restored during Step 5. | Extended lifecycle and claim cells passed at the reviewed Step 5 head and later VM43 suites. | Parent Step 5; Jarvis Step 5 review. |
| `A18` | Every attempted statement on a completed call path receives one terminal disposition, including enforcement refusal. | Remove `rejectRecordedAttempt` or raw outcome recording from pooled, decoded-session, and raw-wire read-only setup failure paths. | `TestReadOnlyEnforcementRefusalTerminalizesRecordedAttempt` and `TestReadOnlyEnforcementRefusalAuditAndHistoryAreAtomic`. | Three subtests reported `history disposition = "running"/"", want "error"`. | All three terminalization paths were restored. | Focused A18 tests pass and assert no target dispatch plus one terminal history result. | Current Step 9 mutation; `core/exec/unitpolicy_disposition_test.go`. |
| `A19` | Statement, portal, segment, and Execute facts invalidate or persist at their declared boundaries. | Four boundary mutations: retain statement classification across replacement; cache first-Bind parameters at statement scope; omit Sync wrap release; remove per-Execute authority admission. | Named-statement reuse, two-portal parameters, candidate-wrap/Sync, and both authority-revocation cells. | Review-recorded RED summary: replacement reused stale facts; two portals shared first-Bind parameters; the next segment inherited the wrap; authority revocation returned `(nil, wanted auth.ErrDenied)`. | Each mutation was inverse-patched in Steps 5-6. | All four scope-specific live witnesses pass in the reviewed VM43 suites. | Parent Steps 5-6; Jarvis Steps 5-6 reviews. |
| `A20` | Exposure migration and orthogonality retain the same seven independent properties as A12. | Repeat the seven A12 mutations independently against migration, ordering, internal-surface, quadrant, plumbing, and authority boundaries. | The seven A12/A20 witnesses named in A12. | The default-open and delayed-gate exact REDs plus the five review-recorded RED summaries are listed in A12. | Current and historical inverse patches are listed in A12. | The combined exposure suite is green locally and in its reviewed VM43 ledgers. | Same current Step 9 and Steps 7a-7c sources as A12. |
| `A21` | Core ignores deny codes, composes no client refusal text, handles novel default rules generically, and preserves pre-disclosure opacity. | Four mutations: inspect `.Code` in core; replace `Reason.Detail` with core-composed text; add a renderer special case for `future-default-rule`; render full reason data pre-disclosure. | AST boundary, structured-reason, novel-rule surface, RPC, and two-mode disclosure cells. | `.Code` mutation: `admission_stages.go:461 inspects .Code in core/exec`; text mutation changed Detail to `exec: refusal composed in core`; renderer mutation returned generic `gate/refused`; disclosure mutation exposed `0A000`, rule, hint, position, and subject. | All four mutations were inverse-patched. | Core, frontdoor, and RPC A21 cells pass. | Current Step 9 mutations; `admission_policy_boundary_test.go`, `admission_stages_test.go`, and surface refusal tests. |
| `A22` | Admission reasons and stages contain no SQLSTATE, pgproto3, or wire vocabulary. | Add `SQLState string` to `admission.Reason`. | `TestAdmissionReasonHasNoProtocolVocabulary` and leaf import boundary. | `admission.Reason field "SQLState" carries protocol vocabulary "sqlstate"`. | The field was removed. | Vocabulary and package-import boundary cells pass. | Current Step 9 mutation; `core/exec/admission_policy_boundary_test.go`. |
| `A23` | Registered admission refusals are actionable by default while explicit opacity and verbatim target errors remain deliberate. | Remove `sizeCapStage{}` from `registeredAdmissionStages`. | Registry parity, RPC mapping completeness, two-mode renderer, duplicate-name, and target-error forwarding cells. | `admission registry differs from production adapters: missing=[sizeCapStage] stale=[]`. | Wanda independently restored the registry entry. | Targeted reviewer verification and the Step 8 VM43 suite passed. | Wanda Step 8 review. |
| `A24` | Target-refused Parse emits its own event and no statement outcome. | Change the target-Parse refusal event to `fd.stmt_outcome`. | `TestPGParseRefused_HasItsOwnEventAndNoStatementOutcome`. | `target-refused Parse emitted fd.stmt_outcome ... Detail:stmt=missing_relation; sqlstate=42P01`. | Wanda independently restored `fd.stmt_parse_refused`. | The live reviewer cell and Step 8 VM43 suite passed. | Wanda Step 8 review. |
| `A25` | Autodb creates no client-visible named object in prepared, portal/cursor, temporary-relation, or savepoint namespaces. | Restore the former prepared parsing probe; inject an autodb cursor/temp relation; add `SAVEPOINT autodb_internal` to an autodb SQL-emission path. | Extended namespace query plus origin-aware SQL observer/source walk. | Former prepared probe reproduced 42P05; temp mutation: `the session carries 1 temporary relation(s) ... autodb_test_temp`; savepoint mutation: `production source wire_sql_observer.go emits named savepoint control "SAVEPOINT autodb_internal"`; cursor namespace is covered by the same extended query. | Prepared probe removal is the incident fix; Wanda restored the temp injection; the savepoint control was removed after the source/runtime RED. | Prepared statement, cursor, temp relation, and named-savepoint witnesses pass. | Incident record; Wanda Step 8 review; current `wire_savepoint_observer_test.go`. |
| `A26` | Reported grammar is re-read before simple Query, extended Parse, and every extended Execute. | Make `runWireGrammarAdmission` return success without checking reported state. | Three live grammar-drift cells. | Simple: `statement after parsing-mode drift reached the target`; Parse: `produced 1 ParseComplete frame(s)`; Execute: `rows=1 completions=1`. | Wanda independently restored the grammar check. | All three live cells passed after restoration and in the Step 8 suite. | Wanda Step 8 review. |
| `A27` | The code-derived surface-by-gate matrix is exhaustive and drives behavioral integration cells. | Bypass `runSessionAdmission` in the production wire-simple dispatch path while leaving matrix data intact. | Structural coordinate walks, 60 no-I/O composition expectations, and pooled/RPC-session/wire-simple/wire-extended production ingress guard cells. | `wire-simple production ingress returned <nil>, want ErrNoWhere`. | The production admission call was restored. | Structural matrix, 15 stable behavioral cases, 60 explicit expectations, and three unconditional production ingress subtests pass locally. The PostgreSQL wire-extended subtest skips without `TEST_PGURL` and must pass in the final VM43 ledger. | Current Step 9 mutation; `core/exec/gate_matrix_test.go`; `docs/admission-gate-matrix.md`. |

## Real corpus replay

- Status: OBSERVED PASS
- Corpus path: `/home/johno/Source/Projects/LabelManager/lm/ddex-delivery-replay/sql/deployments/scripts`
- Manifest SHA-256: `5b11c44fd97e0daef1d4981164de44dfa74667a5bf3ea852245baaa29a0172d3`
- Baseline diff: empty
- `TestCorpusReplay`: PASS
- Files: 470
- Empty files: 10
- Statements: 3577
- Decisions admitted: 2782
- Decisions control: 780
- Decisions nested: 1
- Decisions where: 14
- `TestParseTxControl_CorpusRoundTrip`: PASS
- Transaction controls: 756
- Transaction-control files: 470
- Transaction-control failures: 0
- Mutation RED: admitting the v1compat nested data-modifying CTE changed statement 3 of `000005_update_backfill_contacts.sql` to admitted and raised the admitted census to 2783.
- Restore: the inverse patch returned the exact manifest and census above without regenerating the manifest.

## Machine-checked chain evidence

- Chain document: `docs/admission-chain-comparison.md`.
- Fixed baseline marker: `admission-chain-baseline-aeac553:begin`.
- Production-derived current marker: `admission-chain-current:begin`.
- `core/exec/admission_chain_render_test.go` provides `TestAdmissionChainComparison_BaselineBlockIsFixedAtAeac553`, `TestAdmissionChainComparison_CurrentBlockMatchesProduction`, and `TestAdmissionChainRendering_CriticalStageSlices`.
- `core/exec/admission_ingress_test.go` provides `TestAdmissionChainRendering_ProductionAndRendererShareFactories`, proving that production evaluators and the renderer consume the same stage factories.
- Reordering the production session factory changed both the rendered current block and the critical stage slice; inverse-patching the factory restored both tests.

## Harness provenance

Mutation runs deliberately use dirty worktrees and targeted packages; they prove that a named witness detects a named production change but do not attest a commit. Release evidence is the separate clean, fresh-database, exact-head VM43 `all --race` report. The report is authoritative by commit hash and must be generated after the final Step 9 commit so this document does not create a commit-and-evidence loop.
