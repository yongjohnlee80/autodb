// Package commentguard holds the ratchet that keeps code comments citable.
//
// It has no production code. It exists so the rule — a comment may point at a
// file in this repository or at a public URL, and at nothing else — is a test
// rather than a habit, and so the list of packages that have been read line by
// line lives somewhere a person can see it grow.
//
// See docs/reference/comment-citations.md.
package commentguard
