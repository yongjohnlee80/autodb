/bin/
/dist/
*.test
coverage.out
.env

# Local Go workspace — points at sibling checkouts on THIS machine, so
# it must never be committed (it would break every other clone and CI).
go.work
go.work.sum

# auto-agents workspace mailbox (v0.2.30+)
.auto-agents/
