# lua

The autovim/Neovim integration: plugin spec, full binary lifecycle (cached
binary → checksummed release asset → `go build` fallback, protocol-version
handshake, `:checkhealth`), and the dbase-section renderer glue. Lands at
roadmap milestone M7.

Licensed under [Apache-2.0](../LICENSE).
